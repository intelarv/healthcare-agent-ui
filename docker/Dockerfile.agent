FROM python:3.12-slim

RUN pip install uv

WORKDIR /app

COPY agent/ ./

RUN uv pip install --system -r <(uv pip compile pyproject.toml 2>/dev/null) || uv pip install --system .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
